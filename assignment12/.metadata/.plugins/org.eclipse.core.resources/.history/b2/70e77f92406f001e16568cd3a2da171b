package program05;

import java.util.Scanner;
import java.util.stream.Stream;

public class Que5 {

	public static void main(String[] args) {
	System.out.println("Enter the number to calculate the factcorial");
	long num=new Scanner(System.in).nextLong();
	Stream<long> str=Stream.iterate(1, x->x+1).limit(num);
	
	
	}

}
