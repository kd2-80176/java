package program06;

import java.util.Random;
import java.util.stream.Stream;

public class CalSum {
	public static void main(String[] args) {
		Random r=new Random();
		Stream<r> str=Stream.iterate(1, x->x+1).limit(10);
		
	}

}
